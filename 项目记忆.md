# 项目记忆（给每次新会话的 AI 和健忘的自己）

> 最后更新：2026-09-08 ｜ 本文件是开源库的一部分，保持简洁、只记结论和位置

## 这个项目是什么
里拉竖琴（实物 32.5cm×2.8cm、7 金属弦）的复刻：参数化 3D 建模 + 可打印 STL + 弹奏模拟。

## 当前路线（2026-09-08 定）
1. **建模环境 = Chili3D**（已部署 http://127.0.0.1:8930/ ，源码在 `../chili3d/`）
   - 选型理由：网页版 AI 和用户都能控制；自研 H5 只有 AI 能改、用户插不上手、迭代慢
   - 程序化驱动：`window.__chili = {app, PubSub, Transaction, CommandStore, Serializer}`
     （我们打的补丁，见 `设计文档/CHILI3D_DRIVER.md`，含源码地图和踩坑）
   - 程序化建模范式：`Transaction.execute` 里 `Serializer.deserializeObject(doc, {__cla$$__:"BoxNode",...})`
     → `modelManager.addNode` → `visual.update()`；**不要模拟点击**
2. **几何内核备选 = Manifold**（`lab/`，Node+浏览器双验证通过）
   - Chili3D 用 OCCT（B-rep 全家桶），Manifold 胜在轻量+强制水密；STL 打印件两者皆可
3. **H5 模拟器**（`lyre_simulator.html`）：声学/交互演示与视觉参考，不再往里堆建模功能

## 硬约束
- 打印机热床 250×250mm；模型等比缩放 SCALE=240/325（1:1.35），参数已内嵌
- 琴体壁厚目标 2.4mm；弦不打印（外购金属弦 0.6~1.6mm）；铁扣手工弯
- 材料 PETG（见 docs/MINI_HARPSICHORD_PETG_GUIDE.md）

## 关键决策记录
- ~~超床拆件~~ → 等比缩放解决，无需拆件
- ~~FreeCAD 路线~~ → 降级为备选（FEM/拆件配合才启用）
- ~~纯自研 JSON 建模框架~~ → 改用 Chili3D（原生参数化 JSON 文档 + OCCT），自研只做驱动脚本
- ~~模拟点击操作 UI~~ → 源码级驱动（PubSub/Transaction/直接构造节点）

## 里程碑达成（2026-09-08）
- ✅ `chili3d_lyre_driver.js`：里拉琴体已在 Chili3D 中程序化生成——
  贝塞尔轮廓→polygon wire→face→挤出 20.7mm→音孔+7 穿弦孔 booleanCut→顶面抽壳 2.4mm
  （EditableShapeNode "LyreBody"，水密实体，GUI 可见）
- API 速查见 `../chili3d/DRIVER.md`（XYZ 对象参数 / findSubShapes / 全局 shapeFactory / PubSub 命令通道）

## 下一步
- 用 __chili 驱动接口写里拉琴参数化建模脚本（轮廓 bezier→wire→face→挤出→抽壳→打孔）
- 在 Chili3D 文档树里可由用户改参数实时重算
- 导出 STL/STEP；打印前水密/壁厚检查

## 目录与仓库
- 本目录 = 开源库，remote: github.com:denjhang/harpsichord_3d_js.git
- `../chili3d/` = Chili3D 部署副本（有本地补丁，不推送上游）
- `../harpsichord/` = 用户自己的文件夹，**不要动**
- `../u1HD`、`../u1SX`、`../backups` = 更早的 FreeCAD 路线遗留

## 踩坑速查（详见 docs/H5_STL_WORKFLOW.md §4 和 设计文档/CHILI3D_DRIVER.md）
- 模块顶层 const 依赖顺序（TDZ 静默黑屏）；canvas 网格循环步距越界
- IAB 页面 rAF 会被节流：命中/绘制逻辑不能依赖渲染循环存活
- Manifold: cylinder z 从 0 起算不居中；第 4 参是分段数；status() 是字符串枚举
- Chili3D 导航会被 onbeforeunload 拦截；http.server 缓存强，改完用 ?v=N 强刷
